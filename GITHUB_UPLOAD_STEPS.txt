GitHub quick upload (from project root):

Recommended clean web-offline source to publish:
   generated/github-web-offline-clean/

1) Create empty repo on GitHub (example: applicationversion1-offline)
2) Open that folder, then run:
   git init
   git add .
   git commit -m "web offline clean source"
   git branch -M main
   git remote add origin https://github.com/<YOUR_USER>/<YOUR_REPO>.git
   git push -u origin main

If repo already exists locally:
   git add .
   git commit -m "Update web offline clean source"
   git push
